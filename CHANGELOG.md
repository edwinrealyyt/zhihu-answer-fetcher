# 更新日志 (Changelog)

## [1.1.0] - 2026-08-20

### ✨ 新特性 (New Features)
- **自适应速率与并发引擎**：彻底重构并发模型，引入 `dynConcurrency` 和 `dynDelay`，能够根据网络质量和知乎的反爬虫策略（429/403）动态调整并发数与请求间隔，实现真正的火力全开。
- **预计算并行分页**：首帧获取到总回答数后，直接预先计算出所有的 Offset 并在后台并行分发任务，彻底告别单线程瀑布流排队抓取。
- **实时搜索筛选**：面板新增搜索框，支持对已抓取的回答按“内容”或“作者名”进行实时过滤。
- **数据导出功能**：新增回答数据导出，支持一键将抓取到的回答导出为 JSON、CSV 或 Markdown 格式。
- **一键全展开**：面板新增“一键展开所有回答”按钮，方便快速浏览和全文搜索。
- **全局快捷键**：
  - `J` / `K`: 上下切换回答卡片
  - `Enter`: 展开/收起当前回答
  - `C`: 打开当前回答的评论区
  - `ArrowLeft` / `ArrowRight`: 面板快速翻页

### 🐛 缺陷修复 (Bug Fixes)
- 修复了因为弹窗内事件未正确解绑（如 Esc 快捷键关闭弹窗）导致的内存泄漏问题。
- 修复了二次点击「重新获取」时，因为底层任务状态未能重置而导致的死锁卡死问题。
- 修复了在回答中进行搜索时，意外匹配到 HTML 源码标签的问题（现已在过滤前剥离 HTML 标签）。
- 修复了匿名用户显示文本在不同层级函数内不一致的潜在隐患，全量统一使用中文。
- 大幅清理了代码中的冗余无效计算及过时的 DOM 构建函数（如 `buildGroupedTree`），减少运行开销。
- 修复了多次请求重试堆积造成的性能骤降问题。

### 🎨 体验优化 (UX Improvements)
- 改进了导出弹窗的 UI，提供了更直观的交互。
- 统一了控制台日志的前缀与输出规范，屏蔽了高频刷屏无用信息，减少对开发者工具的干扰。
- 移除了弹窗中略显尴尬的“极速”字眼，回归专业稳重的文案风格。
